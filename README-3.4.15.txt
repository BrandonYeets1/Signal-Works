SIGNAL WORKS CORE 3.4.15 SOURCE PATCH

1. Extract this ZIP directly into the project folder containing build.gradle.
2. Run Apply-SignalWorks-3.4.15.bat.
3. Run gradlew.bat clean build.

This patch changes only the road-sign renderer, two sign-template JSON models, version metadata, and new milestone documentation. It does not copy or replace any textures.

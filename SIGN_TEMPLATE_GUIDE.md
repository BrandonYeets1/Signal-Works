# Signal Works sign-template convention

Each template uses two material slots:

- `#front` — dynamically replaced by the selected road-sign catalog texture.
- `#back` — fixed or manifest-selected sign-back texture.

Square and diamond templates contain only a north-facing front surface and a south-facing rear surface. The diamond is a square plate rotated 45 degrees around Z; its post and mounting hardware are not rotated.
